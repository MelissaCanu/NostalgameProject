@model Nostalgame.Models.Abbonamento

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Abbonamento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdAbbonamento" />
            <div class="form-group">
                <label asp-for="TipoAbbonamento" class="control-label"></label>
                <input asp-for="TipoAbbonamento" class="form-control" />
                <span asp-validation-for="TipoAbbonamento" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CostoAnnuale" class="control-label"></label>
                <input asp-for="CostoAnnuale" class="form-control" />
                <span asp-validation-for="CostoAnnuale" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
